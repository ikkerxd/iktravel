from django.urls import include, path
from . import views

app_name="tours_app"

urlpatterns = [
    # urls para la aplicacion tours
    # path(
    #     r'^$',
    #     views.IndexView.as_view(),
    #     name='index'
    # ),
]
